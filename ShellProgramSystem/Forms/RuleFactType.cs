namespace ShellProgramSystem.Forms
{
    // Перечисление для типа факта правила (правило посылки/правило заключения)
    public enum RuleFactType
    {    
        // Факт посылки    
        PremiseFact,
        // Факт заключения
        ConclusionFact
    }
}
